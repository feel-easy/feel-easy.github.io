<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Feel Easy</title>
  
  
  <link href="https://feel-easy.github.io/atom.xml" rel="self"/>
  
  <link href="https://feel-easy.github.io/"/>
  <updated>2021-03-16T09:41:13.799Z</updated>
  <id>https://feel-easy.github.io/</id>
  
  <author>
    <name>心安</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Ruby block, proc 和 lambda</title>
    <link href="https://feel-easy.github.io/2021/03/16/Ruby-block-proc-%E5%92%8C-lambda/"/>
    <id>https://feel-easy.github.io/2021/03/16/Ruby-block-proc-%E5%92%8C-lambda/</id>
    <published>2021-03-16T09:36:08.000Z</published>
    <updated>2021-03-16T09:41:13.799Z</updated>
    
    <content type="html"><![CDATA[<p>做为热身，从一些简单的例子开始,</p><pre><code>def f1  yieldenddef f2(&amp;p)  p.callenddef f3(p)  p.callendf1 &#123; puts &quot;f1&quot; &#125;f2 &#123; puts &quot;f2&quot; &#125;f3(proc&#123; puts &quot;f3&quot;&#125;)f3(Proc.new&#123;puts &quot;f3&quot;&#125;)f3(lambda&#123;puts &quot;f3&quot;&#125;)</code></pre><p>若你用的是 ruby1.9 及以上的版本，还可以这样,</p><pre><code>f3(-&gt; &#123;puts &quot;f3&quot;&#125;)</code></pre><p>上面是 block, proc 和 lambda 的一些基本用法。</p><h3 id="block"><a href="#block" class="headerlink" title="block"></a>block</h3><p>先说说 block, ruby 中的 block 是方法的一个重要但非必要的组成部分，们可以认为方法的完整定义类似于，</p><pre><code>def f(零个或多个参数, &amp;p)  ...end</code></pre><p>注意&amp;p 不是参数，&amp;p 类似于一种声明，当方法后面有 block 时，会将 block 捕捉起来存放到变量 p 中，如果方法后面没有 block，那么&amp;p 什么也不干。</p><pre><code>def f(&amp;p)endf(1)  #=&gt; 会抛出ArgumentError: wrong number of arguments (1 for 0)异常f()  #=&gt; 没有异常抛出f() &#123; puts &quot;f&quot;&#125; #=&gt; 没有异常抛出</code></pre><p>从上面代码的运行结果可以知道&amp;p 不是参数</p><pre><code>def f(a)  puts aendf(1) &#123; puts 2&#125;  #=&gt; 没有异常抛出，输出1</code></pre><p>所以任何方法后面都可以挂载一个 block，如果你定义的方法想使用 block 做点事情，那么你需要使用 yield 关键字或者&amp;p</p><pre><code>def f1  yieldenddef f2(&amp;p)  p.callend</code></pre><p>此时 f1, f2 执行时后面必须挂一个 block，否则会抛出异常，f1 抛出 LocalJumpError: no block given (yield) 的异常,f2 抛出 NoMethodError: undefined method ‘call’ for nil:NilClass 的异常, ruby 提供了<code>block_given?</code>方法来判断方法后面是否挂了 block，于是们可以这样修改 f1 和 f2，</p><pre><code>def f1 yield if block_given?enddef f2(&amp;p)  p.call if block_given?end</code></pre><p>这样的话,f1 和 f2 后面无论挂不挂 block 都不会抛异常了。 们再来看看 f2 修改前抛出的 NoMethodError: undefined method ‘call’ for nil:NilClass 异常，这种说明当 f2 后面没有挂 block 的时候 p 是 nil，那么们给 f2 挂个 block，再打印出 p，看看 p 究竟是什么，</p><pre><code>def f2(&amp;p)  puts p.class  puts p.inspect  p.callendf2 &#123;&#125; # 输出Proc和类似&lt;Proc:0x007fdc72829780@(irb):21&gt;</code></pre><p>这说明 p 是一个 Proc 实例对象，在 ruby 中，&amp;还可以这么用，<code>[1,2] &amp; [2,3]</code> 或者 <code>puts true if 1 &amp;&amp; 1</code> 或者在某个类中将它作为一个方法名。</p><p>很多 ruby 老鸟会写类似下面的代码,</p><p><code>[&quot;1&quot;, &quot;2&quot;, &quot;3&quot;].map(&amp;:to_i)</code>，其效果和<code>[&quot;1&quot;, &quot;2&quot;, &quot;3&quot;].map &#123;|i| i.to_i &#125;</code>一样, 但简洁了许多，并且更加拉风。 这里的魔法在于符号&amp;会触发:to_i 的 to_proc 方法, to_proc 执行后会返回一个 proc 实例， 然后&amp;会把这个 proc 实例转换成一个 block,们需要要明白 map 方法后挂的是一个 block，而不是接收一个 proc 对象做为参数。&amp;:to_i 是一个 block，block 不能独立存在，同时你也没有办法直接存储或传递它，必须把 block 挂在某个方法后面。</p><p>:to_i 是 <strong>Symbol</strong> 类的实例, <strong>Symbol</strong> 中的 to_proc 方法的实现类似于，</p><pre><code>class Symbol  def to_proc    Proc.new &#123;|obj| obj.send(self) &#125;  endend</code></pre><p>同理们可以给自己写的类定义 to_proc 方法,然后使用&amp;耍下酷，比如，</p><pre><code>class AddBy  def initialize(num = 0)    @num = num  end  def to_proc    Proc.new &#123;|obj| obj.send(&#39;+&#39;, @num)&#125;  endendadd_by_9 = AddBy.new(9)puts [1,2,3].map(&amp;add_by_9) #输出 [10, 11, 12]</code></pre><p>在 ruby 中, block 有形,它有时候是这样</p><pre><code>do |...|  ...end</code></pre><p>有时候是这样</p><pre><code>&#123;|...| ...&#125;</code></pre><p>或者类似 &amp;p, &amp;:to_i, &amp;add_by_9 之类，但是它无体，无体的意思就是说 block 无法单独存在，必须挂在方法后面，并且你没有办法直接把它存到变量里，也没有办法直接将它作为参数传递给方法，所以当你想存储，传递 block 时，你可以使用 proc 对象了，</p><pre><code> p = Proc.new(&amp;:to_i) p = Proc.new &#123;|obj| obj.to_i &#125;  p = Proc.new do |obj|   obj.to_i end p = proc(&amp;:to_i) p = proc &#123;|obj| obj.to_i&#125; p = proc do |obj|   obj.to_i enddef make_proc(&amp;p)  pendp = make_proc(&amp;:to_i)p = make_proc do |obj|  obj.to_iendp = make_proc &#123;|obj| obj.to_i &#125;</code></pre><p>虽然在开发中经常用到 block，但是很少显式地去使用 Proc 或 proc 去实例化 block，比如几乎没有写过这样的代码,</p><pre><code>f(Proc.new &#123;|...| ...&#125;)f(proc &#123;|...| ...&#125;)p =  Proc.new &#123;|...| ...&#125; #然后在某个地方p.call(...)或者将p传递给某个方法，比如f(p)</code></pre><p>在使用 block 时，会忽略 proc 的存在，将 proc 定位为一个幕后的工作者。经常写类似下面的代码，</p><pre><code>def f(...)  ...  yield  ...enddef f(..., &amp;p)  ...  p.call  ...enddef f(..., &amp;p)  instance_eval &amp;p  ...enddef f(..., &amp;p)  ...  defime_method m, &amp;p  ...end</code></pre><p>有些新手会写类似下面的一执行就会报错的代码,</p><pre><code>def f(..., &amp;p)  instance_eval penddef f(..., p)  instance_eval p.callend</code></pre><p>也有这样写的,</p><pre><code>def f(..., &amp;p)  instance_eval do    p.call  endend</code></pre><p>或者</p><pre><code>def f(...)  instance_eval do    yield  endend</code></pre><p>甚至写过类似下面的代码，</p><pre><code>def f(...)  instance_eval yieldend</code></pre><p>我们经常在该挂 block 的时候，却把 proc 对象当参数传给方法了， 或者不明白&amp;p 就是 block 可以直接交给方法使用，曾经也犯过这样的错误就是因为没有把 block 和 proc 正确的区分开来, <strong>&amp;p 是 block, p 是 proc，不到万不得已的情况下不要显式地创建 proc</strong>，每当对 block 和 proc 之间的关系犯糊涂时，就会念上几句。</p><p>再来聊聊 yield 和&amp;p，我们经常这样定义方法，</p><pre><code>def f(...)  yield(...)enddef f(..., &amp;p)  p.call(...)end</code></pre><p>yield 和 call 后面都可以接参数，如果你是这样定义方法</p><pre><code>def f(...)  yield 1, 2end</code></pre><p>那么可以这样执行代码,</p><pre><code>f(...) do |i, j|  puts i  puts jend</code></pre><p>但是这样做也不会有错,</p><pre><code>f(...) do  ...end</code></pre><p>p.call(…) 的情况类似, 也就是说 block 和 proc 都不检查参数 (其实通过 proc 方法创建的 proc 在 1.8 是严格检查参数的，但是在 1.9 及以上的版本是不检查参数的)，为什么 block 和 proc 不检查参数呢?其实这个很好理解，因为在实际应用中你可能需要在一个方法中多次调用 block 或者 proc 并且给的参数个数不一样，比如,</p><pre><code>def f()  yield 0  yield 1, 2enddef ff(&amp;p)  p.call 0  p.call 1, 2, 3endff do |a1, a2, a3|  puts a1     puts a2   puts a3end</code></pre><p>由于方法后面只能挂一个 block，所以要实现上面的代码功能，就不能去严格检查参数了。</p><p>转入正题，这两种方式效果差不多，都能很好地利用 block。使用 yield，看起来简洁，使用&amp;p，看起来直观，并且你可以将&amp;p 传给其他方法使用。 但是在 <strong>ruby1.8</strong> 的版本你不应像下面这样做,</p><pre><code>def f1(...)  eval(&quot;yield&quot;)end</code></pre><p>可以,</p><pre><code>def f2(..., &amp;p)  eval(&quot;p.call&quot;)endf1(...) &#123;&#125;  # 会抛异常f2(...) &#123;&#125;  # 正常运行</code></pre><p>当然上面这种用法很少见，但是却被碰到了，经常写一些方法去请求外部的 api，有时这些外部的 api 不是特别稳定，时不时会遇到一些 bad respoense, timeout 错误，针对这些错误，应该立即重发报文重试，对于其他异常就直接抛异常。于是写了一个 try 方法来满足这个需求，</p><pre><code>  def try(title, options = &#123; &#125;, &amp;p)    ee = &#39;#&#123;e&#125;&#39;    tried_times = 0    max_times = options[:max_times] || 3    exceptions = options[:on] || Exception    exceptions = [exceptions] if !exceptions.is_a?(Array)    rescue_text = &lt;&lt;-EOF      begin        p.call      rescue #&#123;exceptions.join(&#39;,&#39;)&#125; =&gt; e        Rails.logger.info(&quot;#&#123;title&#125;发生异常#&#123;ee&#125;&quot;)        if (tried_times += 1) &lt; max_times          Rails.logger.info(&quot;开始重试#&#123;title&#125;--第#&#123;tried_times&#125;次重试&quot;)          retry         end        raise e      end    EOF    eval rescue_text  endtry(&#39;某某api&#39;, :max_times =&gt; 2, :on =&gt; [Net::HTTPBadResponse, Timeout::Error]) do  open(api_url)end</code></pre><p>最开始用的是 yield，结果在 <strong>ree</strong> 下执行 try 方法时会报错，后来改成使用&amp;p 就通过了。</p><p>通过试验发现在 <strong>ruby1.9</strong> 及以上版本已经没有这种差异了。</p><p>做个小结, <strong>block 和 proc 是两种不同的东西, block 有形无体，proc 可以将 block 实体化, 可以把&amp;p 看做一种运算，其中&amp;触发 p 的 to_proc 方法，然后&amp;会将 to_proc 方法返回的 proc 对象转换成 block</strong> 。</p><p>其中 proc 对象的 to_proc 方法返回自身。</p><pre><code>p = proc &#123;&#125;p.equal? p.to_proc  # 返回true</code></pre><h3 id="lambda"><a href="#lambda" class="headerlink" title="lambda"></a>lambda</h3><p>lambda 是匿名方法, lambda 和 proc 也是两种不同的东西，但是在 ruby 中 lambda 只能依附 proc 而存在，这点和 block 不同，block 并不依赖 proc。</p><pre><code>l = lambda &#123;&#125;puts l.class </code></pre><p>在 <strong>ruby1.8</strong> 中输出的信息类似 #Proc:0x0000000000000000@irb):1( 在 <strong>ruby1.9</strong> 及以上版本输出的信息类似 #Proc:0x007f85548109d0@irb):1lambda)(，注意 <strong>1.9</strong> 及以上版本的输出多了 <strong>(lambda)**，从这里可以理解 ruby 的设计者们确实在有意的区分 lambda 和 proc，并不想把 lambda 和 proc 混在一起,如同 ruby 中没有叫 Block 的类，除非你自己定义一个，ruby 中也没有叫 Lambda 的类，于是将 lambda 对象化的活儿就交给了 Proc，于是令人头大的情况出现了，当你用 lambda 弄出了一个匿名方法时，发现它是一个 proc 对象，并且这个匿名方法能干的活，proc 对象都能做，于是们这些码农不淡定了，<code>Proc.new &#123;&#125;</code>这样可以, <code>proc &#123;&#125;</code>这样也没有问题, <code>lambda &#123;&#125;</code>这样做也不错, <code>-&gt;&#123;&#125;</code>这个还是能行，平时吃个饭都为吃什么左右为难，现在一下子多出了四种差不多的方案来实现同一件事情，确实让人不好选择，特别是有些码农还有点小洁癖，如果在代码里一会儿看到<code>proc&#123;&#125;</code>, 一会儿看到<code>lambda&#123;&#125;</code>,这多不整洁啊，让人心情不畅快。在这里们认定 lambda 或者 -&gt; 弄出的就是一个匿名方法，记做 **l</strong>, 即使它披着 proc 的外衣，proc 或者 Proc.new 创建的就是一个 proc 对象,记做 <strong>p</strong> 在 ruby 各个版本中, <strong>l</strong> 和 <strong>p</strong> 是有一些差别的。</p><p>定义一些方法，</p><pre><code>def f0()  p = Proc.new &#123;return 0&#125;  p.call  1enddef f1()  p = proc &#123; return 0 &#125;  p.call  1enddef f2()  l = lambda &#123; return 0&#125;  l.call  1enddef f3(p)  instance_eval &amp;p  1endclass Aenddef f4(p)  A.class_eval &amp;p  1endp1 = Proc.new &#123; &#125;p2 = proc &#123;&#125;l  = lambda &#123;&#125;def f5()  yield 1, 2endf5 &#123;&#125;f5 &#123;|i|&#125;f5 &#123;|i,j|&#125;f5 &#123;|i,j,k|&#125;</code></pre><p><a href="https://l.ruby-china.com/photo/891b1782bb0db9593b4efda25e2e948e.png"><img src="https://l.ruby-china.com/photo/891b1782bb0db9593b4efda25e2e948e.png" alt="img"></a></p><p>lambda 和 proc 之间的区别除了那个经常用做面试题目的经典的 return 之外，还有一个区别就是 lambda 不能完美的转换为 block(这点可以通过 f3 和 f4 执行的过程得证)，而 proc 可以完美的转换为 block,注意，说的 lambda 指的是用 lambda 方法或者-&gt;符号生成的 proc，当然和方法一样 lambda 是严格检查参数的，这个特点也和 proc 不一样。</p><p>从上面的数据对比来看，在 1.8 版本，lambda 和 proc 方法生成的 proc 对象的行为是一致的，但在 1.9 以上和 jruby 的版本中,lambda 和 proc 的不同处增多，可以认为 ruby 的设计者们并不想把 lambda 和 proc 混同为一件事物。如前面所讲，proc 的主要作用是对象化 block 和 lambda，并且 proc 在行为上更接近于 block。</p><h3 id="retrun-的几个试验"><a href="#retrun-的几个试验" class="headerlink" title="retrun 的几个试验"></a>retrun 的几个试验</h3><pre><code>def f0()  p = Proc.new &#123; return 0&#125;  p.call  1enddef f1()  l = lambda &#123; return 0&#125;  l.call  1endf0 # 返回0f1 # 返回1</code></pre><p>如果你能够理解 proc 在行为上更像 block，lambda 其实就是方法只不过是匿名的，那么你对上面的结果不会感到惊讶。</p><p>如果把 f0,f1 做一些修改，就更容易理解上面的结果了。</p><pre><code>def f0()  return 0  1enddef f1()  def __f1    return 0  end  __f1  1endf0 # 返回0f1 # 返回1</code></pre><p><strong>return 只能在方法体里执行</strong>，</p><pre><code>p = Proc.new &#123; return 0 &#125;l = lambda &#123; return 0 &#125;p.call # 报LocalJumpErrorl.call # 返回0</code></pre><p>构造 p 的时候没有使用 proc {return 0}，因为在 <strong>ruby1.8</strong> 中, proc {return 0}的行为和 lambda 一样，比如在 <strong>ruby1.8</strong> 中，</p><pre><code>def f(p)  p.call  1endp = proc &#123; return 0 &#125;l = lambda &#123; return 0 &#125;f(p)  # 返回1f(l)  # 返回1p.call # 返回0l.call # 返回0def f(p)  p.call  1endp = Proc.new &#123; return 0 &#125;l = lambda.new &#123; return 0&#125;f(p)  # 报LocalJumpErrorf(l)  # 返回1</code></pre><p>感觉 proc 中的 return 能记住 proc 生成时其 block 的位置，然后无论 proc 在哪里调用，都会从 proc 生成时其 block 所在位置处开始 return，有下面的代码为证:</p><pre><code>def f(p)  p.call  1enddef f2()  Proc.new &#123; return 0 &#125;enddef f3()  lambda &#123; return 0 &#125;endp = f2()l = f3()f(p)  # 报LocalJumpError: unexpected return，from (irb):29:in `f2&#39;f(l)  # 返回1</code></pre><p>在 1.8, ree, jruby, 1.9, 2.0 各版本都测试过，结果一样。</p><p>再看看下面的代码,</p><pre><code>def f0(&amp;p)  p.call  1enddef f1()  yield  1endf0 &#123; return 0&#125;  # LocalJumpErrorwf1 &#123; return 0&#125;  # LocalJumpErrorwf0(&amp;Proc.new&#123;return 0&#125;) # LocalJumpErrorf0(&amp;lambda&#123;return 0&#125;)   # 返回1def f2(&amp;p)  penddef f3()  p = f2 do    return 0  end  p.call  1endf3 # 返回0</code></pre><p>们重点看看 f3 中的 proc p，它虽然是从 f2 中生成返回的，但是 p 生成时其 block 是处在在 f3 的方法体内，这个类似于，</p><pre><code>def f3()  p = Proc.new &#123; return 0&#125;  p.call  1end</code></pre><p>所以执行 f3 时，没有异常抛出，返回 0。</p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>当你想写出类似于 f do …; end 或者 f {…}的代码时，请直接使用 block,通过 yield, &amp;p 就能达到目的，当你想使用 proc 时，其实此时绝大部分的情况是你实际想用 lambda,请直接使用 lambda{}或者-&gt;{}就可以了， 尽量不要显示地使用 Proc.new{} 或者 proc{}去创建 proc。</p><p>废话说了一大堆，其实最想说的是: <strong>用 block，用 lambda，不要用 proc，让 proc 做好自己的幕后工作就好了</strong>。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;做为热身，从一些简单的例子开始,&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;def f1
  yield
end

def f2(&amp;amp;p)
  p.call
end

def f3(p)
  p.call
end

f1 &amp;#123; puts &amp;quot;f1&amp;quot; &amp;#</summary>
      
    
    
    
    
    <category term="Ruby" scheme="https://feel-easy.github.io/tags/Ruby/"/>
    
  </entry>
  
  <entry>
    <title>golang 链接redis</title>
    <link href="https://feel-easy.github.io/2021/02/26/golang-%E9%93%BE%E6%8E%A5redis/"/>
    <id>https://feel-easy.github.io/2021/02/26/golang-%E9%93%BE%E6%8E%A5redis/</id>
    <published>2021-02-26T02:58:50.000Z</published>
    <updated>2021-02-26T02:59:31.224Z</updated>
    
    <content type="html"><![CDATA[<h1 id="golang-链接redis"><a href="#golang-链接redis" class="headerlink" title="golang 链接redis"></a>golang 链接redis</h1><h2 id="Redis使用"><a href="#Redis使用" class="headerlink" title="Redis使用"></a>Redis使用</h2><p>使用第三方开源的redis库: github.com/garyburd/redigo/redis</p><p>命令行输入 ：</p><pre><code class="shell">go get github.com/garyburd/redigo/redis</code></pre><h2 id="链接Redis"><a href="#链接Redis" class="headerlink" title="链接Redis"></a>链接Redis</h2><pre><code>package mainimport (    &quot;fmt&quot;    &quot;github.com/garyburd/redigo/redis&quot;)func main() &#123;    c, err := redis.Dial(&quot;tcp&quot;, &quot;localhost:6379&quot;)    if err != nil &#123;        fmt.Println(&quot;conn redis failed,&quot;, err)        return    &#125;     fmt.Println(&quot;redis conn success&quot;)    defer c.Close()&#125;</code></pre><h2 id="String类型Set、Get操作"><a href="#String类型Set、Get操作" class="headerlink" title="String类型Set、Get操作"></a>String类型Set、Get操作</h2><pre><code class="go">package mainimport (    &quot;fmt&quot;    &quot;github.com/garyburd/redigo/redis&quot;)func main() &#123;    c, err := redis.Dial(&quot;tcp&quot;, &quot;localhost:6379&quot;)    if err != nil &#123;        fmt.Println(&quot;conn redis failed,&quot;, err)        return    &#125;    defer c.Close()    _, err = c.Do(&quot;Set&quot;, &quot;abc&quot;, 100)    if err != nil &#123;        fmt.Println(err)        return    &#125;    r, err := redis.Int(c.Do(&quot;Get&quot;, &quot;abc&quot;))    if err != nil &#123;        fmt.Println(&quot;get abc failed,&quot;, err)        return    &#125;    fmt.Println(r)&#125;</code></pre><p>运行结果：</p><pre><code>    MISCONF Redis is configured to save RDB snapshots, but is currently not able to persist on disk. Commands that may modify the data set are disabled. Please check Redis logs for details about the error.</code></pre><p>Redis被配置为保存数据库快照，但它目前不能持久化到硬盘。用来修改集合数据的命令不能用。请查看Redis日志的详细错误信息。</p><p>原因：</p><p>强制关闭Redis快照导致不能持久化。</p><p>解决方案：</p><p>运行config set stop-writes-on-bgsave-error no　命令后，关闭配置项stop-writes-on-bgsave-error解决该问题。</p><p>开启命令行新窗口 2：</p><p>链接Redis：</p><pre><code>    redis-cli -h 127.0.0.1 -p 6379    127.0.0.1:6379&gt; config set stop-writes-on-bgsave-error no    OK</code></pre><p>返回命令行窗口 1 运行程序：</p><pre><code>    go run main.go </code></pre><p>输出结果：</p><pre><code>    100</code></pre><p>命令行窗口 2：</p><pre><code>    127.0.0.1:6379&gt; get abc    &quot;100&quot;</code></pre>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;golang-链接redis&quot;&gt;&lt;a href=&quot;#golang-链接redis&quot; class=&quot;headerlink&quot; title=&quot;golang 链接redis&quot;&gt;&lt;/a&gt;golang 链接redis&lt;/h1&gt;&lt;h2 id=&quot;Redis使用&quot;&gt;&lt;a href=</summary>
      
    
    
    
    
  </entry>
  
  <entry>
    <title>redis 常用命令</title>
    <link href="https://feel-easy.github.io/2021/02/26/redis-%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4/"/>
    <id>https://feel-easy.github.io/2021/02/26/redis-%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4/</id>
    <published>2021-02-26T00:59:34.000Z</published>
    <updated>2021-02-26T01:01:52.117Z</updated>
    
    <content type="html"><![CDATA[<h1 id="redis-常用命令"><a href="#redis-常用命令" class="headerlink" title="redis 常用命令"></a>redis 常用命令</h1><h2 id="1-redis命令"><a href="#1-redis命令" class="headerlink" title="1.redis命令"></a>1.redis命令</h2><pre><code>1 redis执行了make install后，redis的课执行文件都会自动复制到 /usr/local/bin 目录2 redis-server        redis服务器3 redis-cli            redis命令行客户端4 redis-benchmark        redis性能测试工具5 redis-check-aof        aof文件修复工具6 redis-check-dump    rdb文件检查工具</code></pre><h2 id="2-停止Redis命令"><a href="#2-停止Redis命令" class="headerlink" title="2.停止Redis命令"></a>2.停止Redis命令</h2><pre><code># 停止Redis命令redis-cli shutdown</code></pre><h2 id="3-启动-redis-命令"><a href="#3-启动-redis-命令" class="headerlink" title="3.启动 redis 命令"></a>3.启动 redis 命令</h2><h3 id="1-启动-redis-server"><a href="#1-启动-redis-server" class="headerlink" title="(1)启动 redis-server"></a>(1)启动 redis-server</h3><pre><code>1 redis-server</code></pre><h3 id="2-带配置文件启动"><a href="#2-带配置文件启动" class="headerlink" title="(2)带配置文件启动"></a>(2)带配置文件启动</h3><pre><code># 注意配置文件路径redis-server ./redis.conf </code></pre><h3 id="3-带配置文件启动-且指定某几个配置-配置名称前加-–"><a href="#3-带配置文件启动-且指定某几个配置-配置名称前加-–" class="headerlink" title="(3)带配置文件启动 且指定某几个配置 配置名称前加 –"></a>(3)带配置文件启动 且指定某几个配置 配置名称前加 –</h3><pre><code># 配置后台启动，且端口是 1123redis-server ./redis.conf --daemonize yes --port 1123</code></pre><p> 会覆盖配置文件里面的值</p><h2 id="4-redis命令行客户端"><a href="#4-redis命令行客户端" class="headerlink" title="4.redis命令行客户端"></a>4.redis命令行客户端</h2><h3 id="1-发送命令"><a href="#1-发送命令" class="headerlink" title="(1).发送命令"></a>(1).发送命令</h3><pre><code>1 第一种方式2 发送命令，如 redis-cli shutdown3 第二种方式：主要用这种模式4 进入交互模式，默认是127.0.0.1和6379端口5 可以指定 redis-cli -h 127.0.0.1 -p 6379</code></pre><p>注意：框里面的是 6379 里面没有登录成功，下面改成了 1123 成功了，因为刚才上面改了端口</p><h3 id="2-命令返回值"><a href="#2-命令返回值" class="headerlink" title="(2).命令返回值"></a>(2).命令返回值</h3><p>状态回复：pong表示可用</p><p>错误回复：不知道这个什么命令</p><p>整数回复：多种命令会回复整数</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;redis-常用命令&quot;&gt;&lt;a href=&quot;#redis-常用命令&quot; class=&quot;headerlink&quot; title=&quot;redis 常用命令&quot;&gt;&lt;/a&gt;redis 常用命令&lt;/h1&gt;&lt;h2 id=&quot;1-redis命令&quot;&gt;&lt;a href=&quot;#1-redis命令&quot; c</summary>
      
    
    
    
    
  </entry>
  
  <entry>
    <title>乱码数据的处理问题</title>
    <link href="https://feel-easy.github.io/2021/01/28/%E4%B9%B1%E7%A0%81%E6%95%B0%E6%8D%AE%E7%9A%84%E5%A4%84%E7%90%86%E9%97%AE%E9%A2%98/"/>
    <id>https://feel-easy.github.io/2021/01/28/%E4%B9%B1%E7%A0%81%E6%95%B0%E6%8D%AE%E7%9A%84%E5%A4%84%E7%90%86%E9%97%AE%E9%A2%98/</id>
    <published>2021-01-28T06:21:39.000Z</published>
    <updated>2021-01-28T07:51:49.933Z</updated>
    
    <content type="html"><![CDATA[<p>作为一个开发人员，遇见中文乱码问题是常有的事情， 但是 想要复原这些乱码数据的一致是一个让人头疼的问题<br>这里我总结了几个 可以判断乱码原始编码的几个方法<br><strong>比如 拿到一个乱码数据:</strong></p><pre><code class="python">temp = &#39;楀钩镞惰鐪熷涔狅纴鍒昏嫤阍荤爷鎶?湳锛屾墡瀹炲伐浣滐纴瑙勮寖绠＄悊銆备汉镐寲镄勭鐞嗗哜镵氢简涓?镓规妧链骞诧纴寰楀埌浜嗗叏鍏徃锻桦伐镄勪俊璧栥?锻桦伐100%鍙傚姞鍖讳缭锛?5钖嶅憳宸弬锷犲煄淇濓纴澶栨潵锻桦伐鎻愪緵椋熷锛屾椂镞跺埢鍒讳互鍏变骇鍏氩憳镄勬爣鍑呜 閲忚嚜宸憋纴鍦濂戒紒涓氱殑钖屾椂锛屼负绀句细浜嬩笟鎹愯祫锷10涓囧厓锛屾崘璧犺传锲板憳宸灞炴不鐥?涓囧厓锛屽府锷传锲板鐢?.6涓囧厓锛屾崘璧犳枃鏋椾腑瀛绅链鸿澶?涓囧厓锛屾厛锽勬崘鐚?涓囧厓銆?/p&gt; 锻拰鍏村悓蹇楀浜嬩笟镓宪杩芥眰锛屽湪杩椤崄鍑犲勾浼佷笟鍙戝睍杩囩涓纴鍏呭垎鍙戞尌浜嗕竴涓叡浜厷锻樼殑鍏堥攱妯寖甯浣灭敤锛屽弹鍒颁简闀囨敛搴溿?甯傜骇涓荤閮桊镄勮褰帮纴绀句细钖勭晫镄勪竴镊村璇勶纴浠栧潥淇彧链変笉鏂彂灞曪&#39;</code></pre><p>首先要判断字符串的原始编码,python 中的 chardet 可以判断文件的编码，当然如果文本被重新编码过了就不能够判断原来的编码了</p><pre><code class="python">import chardetdata = open(&#39;文件&#39;, &#39;rb&#39;)dicts = chardet.detect(data) dicts # &#123;&#39;encoding&#39;: &#39;utf-8&#39;, &#39;confidence&#39;: 0.99, &#39;language&#39;: &#39;&#39;&#125;</code></pre><p>不过根据经验来说，由于微软对于国内的操作系统使用的是gbk 编码的，可以尝试用gbk编码再使用utf-8再解码, 不过已经再编码过的文本，即便这么操作也很难完全恢复原来的文本</p><pre><code class="python">temp.encode(&#39;gbk&#39;).decode(&#39;utf-8&#39;,&#39;ignore&#39;)                                                                                                                                                               # &#39;平߶真习刻苦Իү?，扎实工作规范管理〱䌖Є理K?ع技干得到了全兏뷥Є信赖?뷥100%参加医?5薍员巏ɠ城保外来뷥提供食，时߶刻刻以共产벑Є标 量自己好企业的薌时，为社ϸ事业捐资10万元，捐赠贫ư员属治?万元，帮ɴư?.6万元，捐赠文林中?万元，慈B捐?万元?/p&gt; ͒兴同志事业追求，在額几年企业发展过充分发挥了一丅亅͘的先锋樌，嵯到镇府?市级主Є彰社ϸ薄界Є一评他坚俏不斏展&#39;</code></pre><p>当然也可以尝试 gb2312，gb18030，big5 等等多种编码再解码</p><p>ruby也有相应的方法</p><pre><code class="ruby">require &#39;iconv&#39;Iconv.iconv(&quot;gbk//IGNORE&quot;,&quot;UTF-8//IGNORE&quot;,temp)</code></pre>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;作为一个开发人员，遇见中文乱码问题是常有的事情， 但是 想要复原这些乱码数据的一致是一个让人头疼的问题&lt;br&gt;这里我总结了几个 可以判断乱码原始编码的几个方法&lt;br&gt;&lt;strong&gt;比如 拿到一个乱码数据:&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;py</summary>
      
    
    
    
    <category term="问题" scheme="https://feel-easy.github.io/categories/%E9%97%AE%E9%A2%98/"/>
    
    
    <category term="ruby" scheme="https://feel-easy.github.io/tags/ruby/"/>
    
    <category term="python" scheme="https://feel-easy.github.io/tags/python/"/>
    
  </entry>
  
  <entry>
    <title>python虚拟环境virtualenv</title>
    <link href="https://feel-easy.github.io/2021/01/08/pythonvirtualenv/"/>
    <id>https://feel-easy.github.io/2021/01/08/pythonvirtualenv/</id>
    <published>2021-01-08T07:30:22.000Z</published>
    <updated>2021-01-08T06:08:27.037Z</updated>
    
    <content type="html"><![CDATA[<p>pip, virtualenv, fabric通称为pythoner的三大神器。</p><p>virtualenv——用来建立一个虚拟的python环境，一个专属于项目的python环境。用virtualenv 来保持一个干净的环境非常有用</p><p>测试环境：linux下</p><h3 id="1、基本使用"><a href="#1、基本使用" class="headerlink" title="1、基本使用"></a>1、基本使用</h3><p>  通过pip安装virtualenv：</p><pre><code class="shell">pip install virtualenv</code></pre><p>  测试安装:</p><pre><code class="shell">virtualenv --version</code></pre><p>  为一个工程项目搭建一个虚拟环境:</p><pre><code class="shell">cd my_projectvirtualenv my_project_env</code></pre><p>  另外，如果存在多个python解释器，可以选择指定一个Python解释器（比如<code>python2.7</code>），没有指定则由系统默认的解释器来搭建： </p><pre><code class="shell">virtualenv -p /usr/bin/python2.7 my_project_env</code></pre><p>  将会在当前的目录中创建一个名my_project_env的文件夹，这是一个独立的python运行环境，包含了Python可执行文件， 以及 <code>pip</code> 库的一份拷贝，这样就能安装其他包了，不过已经安装到系统Python环境中的所有第三方包都不会复制过来，这样，我们就得到了一个不带任何第三方包的“干净”的Python运行环境来。</p><p> 要开始使用虚拟环境，其需要被激活：</p><pre><code class="shell">source my_project_env/bin/activate</code></pre><p>停用虚拟环境：</p><pre><code class="shell">deactivate</code></pre><p>停用后将回到系统默认的Python解释器</p><h3 id="2、其他"><a href="#2、其他" class="headerlink" title="2、其他"></a>2、其他</h3><p>用pip freeze查看当前安装版本</p><pre><code class="shell">pip freeze</code></pre><p>另外：</p><pre><code class="shell">pip freeze &gt; requirements.txt</code></pre><p>这将会创建一个 <code>requirements.txt</code> 文件，其中包含了当前环境中所有包及 各自的版本的简单列表。您可以使用 “pip list”在不产生requirements文件的情况下， 查看已安装包的列表。这将会使另一个不同的开发者（或者是您，如果您需要重新创建这样的环境） 在以后安装相同版本的相同包变得容易。</p><pre><code class="shell">pip install -r requirements.txt</code></pre><p>  这能帮助确保安装、部署和开发者之间的一致性。</p><h2 id="virtualenvwrapper"><a href="#virtualenvwrapper" class="headerlink" title="virtualenvwrapper"></a>virtualenvwrapper</h2><p>提供了一系列命令使得和虚拟环境工作变得愉快许多。它把您所有的虚拟环境都放在一个地方。</p><ol><li>将您的所有虚拟环境在一个地方。</li><li>包装用于管理虚拟环境（创建，删除，复制）。</li><li>使用一个命令来环境之间进行切换。</li></ol><h3 id="1、安装"><a href="#1、安装" class="headerlink" title="1、安装"></a>1、安装</h3><p>  安装（确保 <strong>virtualenv</strong> 已经安装了）：</p><pre><code class="shell">pip install virtualenvwrapperexport WORKON_HOME=~/Envs  #设置环境变量mkdir -p $WORKON_HOME #创建虚拟环境管理目录find / -name virtualenvwrapper.sh #找到virtualenvwrapper.sh的路径source 路径 #激活virtualenvwrapper.sh</code></pre><p>默认virtualenvwrapper安装在下面python解释器中的site-packages，实际上需要运行virtualenvwrapper.sh文件才行；所以需要先进行配置一下：</p><ul><li><code>找到virtualenvwrapper.sh的路径：find / -name virtualenvwrapper.sh</code></li><li>运行virtualenvwrapper.sh文件：source 路径</li></ul><pre><code class="shell">export WORKON_HOME=~/.virtualenvsexport VIRTUALENVWRAPPER_PYTHON=/usr/bin/python3source /home/l/.local/bin/virtualenvwrapper.sh</code></pre><p><strong>ps：每次要想使用virtualenvwrapper 工具时，都必须先激活virtualenvwrapper.sh,另外，如果创建前要将即将的环境保存到Envs中，就要先设置一下环境变量：</strong>export WORKON_HOME=~/Envs，再搭建</p><p>对于Windows，您可以使用 virtualenvwrapper-win</p><p>  安装（确保 <strong>virtualenv</strong> 已经安装了）：</p><pre><code class="shell">pip install virtualenvwrapper-win在Windows中，WORKON_HOME默认的路径是 %USERPROFILE%Envs 。</code></pre><h3 id="2、基本使用"><a href="#2、基本使用" class="headerlink" title="2、基本使用"></a>2、基本使用</h3><p>1、创建一个虚拟环境：</p><pre><code class="shell">mkvirtualenv project_env</code></pre><p><code>这会在Envs</code> 中创建 project_env虚拟环境</p><p>　  选择一个python解释器来搭建：</p><pre><code class="shell">mkvirtualenv env  --python=python2.7</code></pre><p>2、在虚拟环境上工作：</p><pre><code class="shell"> workon project_env</code></pre><p>或者，您可以创建一个项目，它会创建虚拟环境，并在 <code>$WORKON_HOME</code> 中创建一个项目目录。 当您使用 <code>workon </code>project_env 时，会 <code>cd</code> -ed 到项目目录中。</p><pre><code class="shell">mkvirtualenv project_env</code></pre><p><strong>virtualenvwrapper</strong> 提供环境名字的tab补全功能。当您有很多环境， 并且很难记住它们的名字时，这就显得很有用。</p><p><code>workon</code> 也能停止您当前所在的环境，所以您可以在环境之间快速的切换。</p><p>3、停止虚拟环境</p><pre><code class="shell">deactivate</code></pre><p>4、删除：</p><pre><code class="shell"> rmvirtualenv project_env</code></pre><h3 id="3、其他有用的命令"><a href="#3、其他有用的命令" class="headerlink" title="3、其他有用的命令"></a>3、其他有用的命令</h3><pre><code class="shell">lsvirtualenv    #列举所有的环境。cdvirtualenv    #导航到当前激活的虚拟环境的目录中，比如说这样您就能够浏览它的 site-packages。cdsitepackages   # 和上面的类似，但是是直接进入到 site-packages 目录中。lssitepackages     #显示 site-packages 目录中的内容。</code></pre><p><a href="https://virtualenvwrapper.readthedocs.io/en/latest/command_ref.html">virtualenvwrapper 命令的完全列表</a> 。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;pip, virtualenv, fabric通称为pythoner的三大神器。&lt;/p&gt;
&lt;p&gt;virtualenv——用来建立一个虚拟的python环境，一个专属于项目的python环境。用virtualenv 来保持一个干净的环境非常有用&lt;/p&gt;
&lt;p&gt;测试环境：lin</summary>
      
    
    
    
    <category term="笔记" scheme="https://feel-easy.github.io/categories/%E7%AC%94%E8%AE%B0/"/>
    
    
    <category term="python" scheme="https://feel-easy.github.io/tags/python/"/>
    
  </entry>
  
  <entry>
    <title>FactoryBot 使用总结</title>
    <link href="https://feel-easy.github.io/2021/01/08/FactoryBot%20%E4%BD%BF%E7%94%A8%E6%80%BB%E7%BB%93/"/>
    <id>https://feel-easy.github.io/2021/01/08/FactoryBot%20%E4%BD%BF%E7%94%A8%E6%80%BB%E7%BB%93/</id>
    <published>2021-01-08T06:55:31.000Z</published>
    <updated>2021-01-08T06:08:35.829Z</updated>
    
    <content type="html"><![CDATA[<p>1、四种创建方法</p><pre><code class="ruby"># 返回对象 但是不保存user = build(:user)# 返回对象并且保存user = create(:user)# 返回可用于构建用户实例的属性 HASHattrs = attributes_for(:user)# 返回一个对象，该对象的所有定义属性都已存根stub = build_stubbed(:user)# 将一个块传递给上面的任何方法都将产生返回对象create(:user) do |user|  user.posts.create(attributes_for(:post))end</code></pre><p>2、覆盖属性</p><pre><code class="ruby"># Build a User instance and override the first_name propertyuser = build(:user, first_name: &quot;Joe&quot;)user.first_name# =&gt; &quot;Joe&quot;</code></pre><p>3、重命名</p><pre><code class="ruby">factory :user, aliases: [:author, :commenter] do  first_name &#123; &quot;John&quot; &#125;  last_name &#123; &quot;Doe&quot; &#125;  date_of_birth &#123; 18.years.ago &#125;endfactory :post do  author  # instead of  # association :author, factory: :user  title &#123; &quot;How to read a book effectively&quot; &#125;  body &#123; &quot;There are five steps involved.&quot; &#125;endfactory :comment do  commenter  # instead of  # association :commenter, factory: :user  body &#123; &quot;Great article!&quot; &#125;end</code></pre><p>4、属性依赖</p><pre><code class="ruby">factory :user do  first_name &#123; &quot;Joe&quot; &#125;  last_name  &#123; &quot;Blow&quot; &#125;  email &#123; &quot;#&#123;first_name&#125;.#&#123;last_name&#125;@example.com&quot;.downcase &#125;endcreate(:user, last_name: &quot;Doe&quot;).email# =&gt; &quot;joe.doe@example.com&quot;</code></pre><p>5、通过嵌套工厂，您可以轻松地为同一个类创建多个工厂，而无需重复常见的属性:</p><pre><code class="ruby">factory :post do  title &#123; &quot;A title&quot; &#125;  factory :approved_post do    approved &#123; true &#125;  endendapproved_post = create(:approved_post)approved_post.title    # =&gt; &quot;A title&quot;approved_post.approved # =&gt; true</code></pre><pre><code class="ruby">factory :post do  title &#123; &quot;A title&quot; &#125;endfactory :approved_post, parent: :post do  approved &#123; true &#125;end</code></pre><p>6、在factory_bot 5中，关联默认使用与其父对象相同的构建策略:</p><pre><code class="ruby">FactoryBot.define do  factory :author  factory :post do    author  endendpost = build(:post)post.new_record?        # =&gt; truepost.author.new_record? # =&gt; truepost = create(:post)post.new_record?        # =&gt; falsepost.author.new_record? # =&gt; false</code></pre>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;1、四种创建方法&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;ruby&quot;&gt;# 返回对象 但是不保存
user = build(:user)

# 返回对象并且保存
user = create(:user)

# 返回可用于构建用户实例的属性 HASH
attrs = att</summary>
      
    
    
    
    <category term="笔记" scheme="https://feel-easy.github.io/categories/%E7%AC%94%E8%AE%B0/"/>
    
    
    <category term="ruby" scheme="https://feel-easy.github.io/tags/ruby/"/>
    
  </entry>
  
  <entry>
    <title>yield 的基本使用</title>
    <link href="https://feel-easy.github.io/2021/01/08/yield/"/>
    <id>https://feel-easy.github.io/2021/01/08/yield/</id>
    <published>2021-01-08T06:49:22.000Z</published>
    <updated>2021-01-08T06:01:19.198Z</updated>
    
    <content type="html"><![CDATA[<p>所有的”方法（methods）”隐式跟上一个”块（block）”参数。</p><p>块参数也可以明确给定，形式就是在参数前面加一个”&amp;”，比如 def fn(arg1, arg2, &amp;block) end，其中的 &amp;block 就是明确给定的块参数。</p><p>块参数的动作，可以通过调用 call() 方法执行，还可以用 yield 来执行 —— yield 其实就是一个语法糖。</p><p>所以以下几种写法常常是等价的：</p><pre><code class="ruby">#method receives an invisible block argumentdef foo    yield 1end#specify it explicitlydef foo2(&amp;block)    yield 1end#yield is equal to block.calldef foo3(&amp;block)    block.call(1)    end#function callfoo1 &#123;|x| puts x&#125;    # =&gt; 1foo2 &#123;|x| puts x&#125;    # =&gt; 1foo3 &#123;|x| puts x&#125;    # =&gt; 1</code></pre><h2 id="Proc"><a href="#Proc" class="headerlink" title="Proc"></a>Proc</h2><p>前面说到所有方法都可以隐式或显式指定一个块参数，那么块参数到底是什么呢？</p><p>答案是 Proc 对象，一个具有 call 方法的对象。</p><p>Proc 对象的定义有几种形式：</p><ul><li>直接使用 {}</li><li>使用 Proc.new {}</li><li>使用 proc {}</li><li>使用 lambda {}</li></ul><pre><code class="ruby">#yield is equal to block.calldef foo(&amp;block)    puts block.class    puts block.to_s    yield 1    end#function call# Proc created using &#123;&#125; syntaxfoo &#123;|x| puts x&#125;   # =&gt; Proc# =&gt; #&lt;Proc:0x00000000e0b140@(ruby):9&gt;# =&gt; 1# Proc created with the &quot;proc&quot; keyword. Note &amp; syntax when calling.my_proc = proc &#123; |n| puts n &#125;foo(&amp;my_proc)# =&gt; Proc# =&gt; #&lt;Proc:0x00000000e0b140@(ruby):12&gt;# =&gt; 1# Proc creates with Proc.newmy_proc = Proc.new &#123; |n| puts n &#125;foo(&amp;my_proc)    # =&gt; 1# =&gt; Proc# =&gt; #&lt;Proc:0x00000000e0b140@(ruby):16&gt;# =&gt; 1# Proc created with the &quot;lambda&quot; keyword. Nearly same thing.my_proc = lambda &#123; |n| puts n &#125;foo(&amp;my_proc)# =&gt; Proc# =&gt; #&lt;Proc:0x00000000e0b140@(ruby):20 (lambda)&gt;# =&gt; 1</code></pre><h2 id="yield-self"><a href="#yield-self" class="headerlink" title="yield self"></a>yield self</h2><p>在一个对象中，self 表示是一个当前对象的引用。</p><p>所以，常见的 yield self if block_given? 中的 self 就和其它地方使用 self 一样，没什么特殊的。</p><pre><code class="ruby">class C1    def foo(&amp;block)        puts block.class        puts block.to_s        yield self if block_given?        yield &quot;AAAAAAAAA&quot;    endendclass C2    def foo(&amp;block)        puts block.class        puts block.to_s        yield self if block_given?        yield &quot;BBBBBBBBB&quot;    end    def to_s        &quot;XXXXXXXXXX&quot;    endendc1 = C1.newc1.foo  &#123;|x| puts x&#125;# =&gt; Proc# =&gt; #&lt;Proc:0x00000001c84aa0@(ruby):23&gt;# =&gt; #&lt;Context::C1:0x00000001c84af0&gt;# =&gt; AAAAAAAAAc2 = C2.newc2.foo  &#123;|x| puts x&#125;# =&gt; Proc# =&gt; #&lt;Proc:0x00000001c842f8@(ruby):26&gt;# =&gt; XXXXXXXXXX# =&gt; BBBBBBBBB</code></pre><h2 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h2><p>method 定义中 &amp;block 参数必须在最后</p><pre><code class="ruby"># 正确示例def foo(arg1, arg2, &amp;block)    puts blockend#function callblock = proc &#123;|x| puts x&#125;foo( 1, 2, &amp;block)                   # =&gt; #&lt;Proc:0x000000011f3aa0@(ruby):14&gt;#错误示例def foo(arg1, &amp;block, arg2)     # =&gt; (ruby): syntax error    puts blockend</code></pre><p>yield 相当于是 block.call() 方法的调用，所以参数个数也需要对应</p><pre><code class="ruby">def foo()    yield 1,2,3     # 这里的 1 2 3 就是传递的参数end#function callfoo &#123;|x| puts x&#125;        # =&gt; 1foo &#123;|x,y,z| puts z&#125;    # =&gt; 3foo &#123;|x,y,z,k| puts k&#125;  # 为空</code></pre>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;所有的”方法（methods）”隐式跟上一个”块（block）”参数。&lt;/p&gt;
&lt;p&gt;块参数也可以明确给定，形式就是在参数前面加一个”&amp;amp;”，比如 def fn(arg1, arg2, &amp;amp;block) end，其中的 &amp;amp;block 就是明确给定的块参数</summary>
      
    
    
    
    <category term="笔记" scheme="https://feel-easy.github.io/categories/%E7%AC%94%E8%AE%B0/"/>
    
    
    <category term="ruby" scheme="https://feel-easy.github.io/tags/ruby/"/>
    
  </entry>
  
  <entry>
    <title>Hexo 使用简介</title>
    <link href="https://feel-easy.github.io/2021/01/07/new-blog/"/>
    <id>https://feel-easy.github.io/2021/01/07/new-blog/</id>
    <published>2021-01-07T02:49:22.000Z</published>
    <updated>2021-01-08T06:08:10.888Z</updated>
    
    <content type="html"><![CDATA[<p>Hexo 是一个快速、简洁且高效的博客框架。Hexo 使用 Markdown（或其他渲染引擎）解析文章，在几秒内，即可利用靓丽的主题生成静态网页。<br>大大降低了建站的成本。</p><h3 id="创建"><a href="#创建" class="headerlink" title="创建"></a>创建</h3><pre><code class="bash">hexo new &quot;My New Post&quot;</code></pre><h3 id="运行"><a href="#运行" class="headerlink" title="运行"></a>运行</h3><pre><code class="bash">hexo serverhexo s</code></pre><h3 id="生成静态文件"><a href="#生成静态文件" class="headerlink" title="生成静态文件"></a>生成静态文件</h3><pre><code class="bash">hexo generatehexo g</code></pre><h3 id="发布"><a href="#发布" class="headerlink" title="发布"></a>发布</h3><pre><code class="bash">hexo deployhexo d</code></pre><p><a href="https://hexo.io/">Hexo官网</a><br><a href="https://hexo.io/docs/">Hexo 官方文档</a><br><a href="https://hexo.io/zh-cn/docs/troubleshooting.html">问题解答</a><br><a href="https://github.com/hexojs/hexo/issues">GitHub</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;Hexo 是一个快速、简洁且高效的博客框架。Hexo 使用 Markdown（或其他渲染引擎）解析文章，在几秒内，即可利用靓丽的主题生成静态网页。&lt;br&gt;大大降低了建站的成本。&lt;/p&gt;
&lt;h3 id=&quot;创建&quot;&gt;&lt;a href=&quot;#创建&quot; class=&quot;headerlink&quot;</summary>
      
    
    
    
    <category term="笔记" scheme="https://feel-easy.github.io/categories/%E7%AC%94%E8%AE%B0/"/>
    
    
    <category term="教程" scheme="https://feel-easy.github.io/tags/%E6%95%99%E7%A8%8B/"/>
    
  </entry>
  
</feed>
